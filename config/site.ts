export const siteConfig = {
  name: "JobTracker",
  description:
    "Job Progress Tracker for Daily Workers — attendance, tasks, and payroll.",
  url: process.env.NEXT_PUBLIC_SITE_URL ?? "http://localhost:3000",
};
